image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir public
    - cp *.html public/
    - cp static public/
    - cp .well-known public/
  artifacts:
    paths:
    - public
  only:
  - master
