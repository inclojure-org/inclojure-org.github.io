image: clojure:lein-2.8.1-alpine

cache:
  paths:
    - .local-m2

pages:
  stage: deploy
  before_script:
    - lein clean
    - lein cljsbuild once min
  script:
    - mkdir -p public/staging
    - cp -R resources/public/* public/staging
    - cp -R .well-known public/staging
  artifacts:
    paths:
      - public
  only:
    - redesign
