image: alpine:latest

pages:
  stage: deploy
  script:
    - echo 'Copying artifacts to public/'
    - mv *.html static/ public/
    - echo 'Done.'
  artifacts:
    paths:
    - public
  only:
  - master
